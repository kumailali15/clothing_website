services:
  - type: web
    name: shopco-fullstack
    runtime: node
    buildCommand: npm run build && cd backend && npm install
    startCommand: cd backend && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
